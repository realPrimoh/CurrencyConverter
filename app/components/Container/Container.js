import React, { PropTypes } from 'react';
import { View } from 'react-native';

const Container = () => (
    <View>
        {children}
    </View>
);

<Container>
    <Text />
</Container>